# vim: set ft=conf:

update () {
    sudo xbps-install -Su && date +%s  > ~/.lastupdate
}

alias Rs='xbps-query -Rs'
alias sxi='sudo xbps-install'
alias sxr='sudo xbps-remove -R'
alias ssv='sudo sv'
alias xbps-why='xbps-query --fulldeptree  -X'
